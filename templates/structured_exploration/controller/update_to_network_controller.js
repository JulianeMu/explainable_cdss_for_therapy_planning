/**
 *
 * @param patient_id
 */
function update_network_to_current_patient(patient_id) {
    
}

/**
 *
 * @param patient_id
 * @param network_name
 */
function update_network_to_current_patient(patient_id, network_name, net_version) {
    //get_net_by_name_patientID_version(callback, network_name, patient_id, net_version, username, password, true);
}